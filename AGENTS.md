# GoDocs CLI Guidance

- Before making any automation changes or running commands in this folder, read `Desktop/Yard/tools/README.md` so you know the shared registry entry for GoDocs CLI.
- Follow the `Quick checklist` in `TOOL.md` and keep the shared registry docs aligned whenever you adjust the CLI, config paths, or agent-facing recipes.
